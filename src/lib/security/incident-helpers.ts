/**
 * Security Incident Response - Helper Functions
 *
 * This module contains standalone helper functions used by the incident
 * response system. Currently the incident response logic is encapsulated
 * within the IncidentResponseManager class, so this module serves as the
 * future home for any pure, stateless helpers that are extracted from
 * the manager.
 */

// Placeholder for standalone incident response helpers.
//
// All current response logic lives as methods on `IncidentResponseManager`
// (see ./incident-response.ts). As pure helpers are identified they should
// be moved here so they can be unit tested in isolation and reused
// without instantiating the manager.
