// CSS/style mock for Jest. Returns an empty object for CSS/SCSS/SASS imports
// so component tests don't need the actual stylesheets (or the missing
// identity-obj-proxy package on corrupted network drives).
module.exports = {}
